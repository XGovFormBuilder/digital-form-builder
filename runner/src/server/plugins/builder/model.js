// const joi = require('joi')
const { EngineModel } = require('@xgovformbuilder/engine')

class Model extends EngineModel {
  makeSchema (state) {
    return super.makeSchema(state)
  }
}

module.exports = Model
